<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.EyePhone1" parent="Base.Theme.EyePhone">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:windowSplashScreenBackground">@color/main_eyephone</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/eyephone_logo2</item>
    </style>
</resources>